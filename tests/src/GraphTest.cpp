//
// Created by nrx on 22.05.2020.
//

#include "Graph.h"
#include <gtest/gtest.h>
/*
TEST(GraphIterTest, test1) {
    Graph<Empty, Empty> g;
    auto n1 = g.addNodeInBack(Empty{});
    auto n2 = g.addNodeInBack(Empty{});
    auto n3 = g.addNodeInBack(Empty{});
    auto n4 = g.addNodeInBack(Empty{});
    auto n5 = g.addNodeInBack(Empty{});

    g.addLink(n1, n2, Empty{});
    g.addLink(n2, n4, Empty{});
    g.addLink(n2, n5, Empty{});
    g.addLink(n1, n3, Empty{});
    auto n = g.begin();
    ASSERT_EQ(n.getIndex(), 0);
    n.next();
    ASSERT_EQ(n.getIndex(), 1);
    n.next();
    ASSERT_EQ(n.getIndex(), 3);
    n.next();
    ASSERT_EQ(n.getIndex(), 4);
    n.next();
    ASSERT_EQ(n.getIndex(), 2);

}


TEST(GraphIterTest, test3) {
    Graph<Empty, Empty> g;
    auto n1 = g.addNodeInBack(Empty{});
    auto n2 = g.addNodeInBack(Empty{});
    auto n3 = g.addNodeInBack(Empty{});
    auto n4 = g.addNodeInBack(Empty{});
    auto n5 = g.addNodeInBack(Empty{});

    g.addLink(n1, n2, Empty{});
   //
    g.addLink(n2, n5, Empty{});
    g.addLink(n1, n3, Empty{});
    auto n = g.begin();
    ASSERT_EQ(n.getIndex(), 0);
    n.next();
    ASSERT_EQ(n.getIndex(), 1);

    n.next();
    ASSERT_EQ(n.getIndex(), 4);
    n.next();
    ASSERT_EQ(n.getIndex(), 2);
    n.next();
    ASSERT_EQ(n.getIndex(), 3);

}*/
